node_modules/

# scratch worktrees / sibling agent checkouts (never part of this branch)
.worktrees/

# internal tier — candid dev docs live here, never tracked, never published
internal/
CLAUDE.local.md

# Python reference build artifacts (the reference lives on main, not this branch)
__pycache__/
*.pyc
docs/_myreport.html

# machine-local release-queue state (operator tooling) — never tracked
.elephant/

# Agent context — internal tier, never published (verify.mjs internal-docs gate)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
